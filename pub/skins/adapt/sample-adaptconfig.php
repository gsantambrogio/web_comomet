<?php if (!defined('PmWiki')) exit();
/**
* adaptconfig.php - Adapt Skin configuration file 
*/

##  These control whether or not the site header and page title are shown
##  on the edit page. Hiding one or both of them moves the edit screen
##  textarea upward, which can improve the page-editing experience.
#
##  hide the masthead (displayed by default)
# $AdaptEditHeaderEna = 0;
#
##  show the page title (hidden by default)
# $AdaptEditTitleEna = 1;



## You can have a this-skin-only custom site logo.
## $PubDirUrl refers to the pub directory (pub/ by default)
## $SkinDirUrl refers to the skin directory (pub/skins/adapt/ by default)
# 
# $PageLogoUrl = "$PubDirUrl/AdaptSkinCustomSiteLogo.png";

##  By default the skin inserts two <link> tags in the HTML header
##  pointing to a default shortcut/favorites icon (favicon.ico) that
##  appears in bookmarks (favorites), browser tabs, and elsewhere.
#
# AdaptFaviconEna = 0;   // 0 disables the links
#
##  AdaptFaviconUrl is the URL for a custom favicon file.
##  Use an icon with the .ico (image/x-icon) format.
##  You can create a custom icon at http://favicon.cc/
# 
# AdaptFaviconUrl = "$PubDirUrl/favicon.ico";

